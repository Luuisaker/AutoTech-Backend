AutoTech — Alembic Migrations

Single migration (001_initial) creates all tables.
To create a fresh DB: alembic upgrade head
To tear down:        alembic downgrade base

New migrations: alembic revision --autogenerate -m "description"